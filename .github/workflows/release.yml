name: Build and publish STORMFORGED

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - README.md
      - SOURCES.md
      - COMPANIONS.md
      - RELEASE_NOTES.md
      - cart.json
      - upstreams.json
      - tools/make_label.py
      - tools/update_pins.py
      - .github/workflows/release.yml
      - .github/workflows/update-pins.yml

permissions:
  contents: write

concurrency:
  group: stormforged-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve cart version
        shell: bash
        run: |
          VERSION="$(python3 -c 'import json; print(json.load(open("cart.json"))["version"])')"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "TAG=v$VERSION" >> "$GITHUB_ENV"
          echo "CART=kanto_stormforged-$VERSION.g1rcart" >> "$GITHUB_ENV"
          echo "PACKAGE=KANTO_STORMFORGED_v$VERSION.zip" >> "$GITHUB_ENV"

      - name: Generate cartridge artwork
        run: python3 tools/make_label.py label.png

      - name: Fetch official Gen1Recomp cartkit
        run: |
          curl -fsSL --retry 3 \
            -o "$RUNNER_TEMP/cartkit.py" \
            https://raw.githubusercontent.com/bryanthaboi/gen1recomp/dev/tools/cartkit.py
          python3 "$RUNNER_TEMP/cartkit.py" selftest --quiet

      - name: Validate and pack cart
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          mkdir -p dist
          python3 "$RUNNER_TEMP/cartkit.py" validate . --strict --online
          python3 "$RUNNER_TEMP/cartkit.py" pack . --online -o "dist/$CART"

      - name: Build complete distribution ZIP
        run: |
          rm -rf staging
          mkdir -p staging/KANTO_STORMFORGED
          cp README.md SOURCES.md COMPANIONS.md RELEASE_NOTES.md cart.json upstreams.json label.png "dist/$CART" staging/KANTO_STORMFORGED/
          (cd staging && zip -9 -r "../dist/$PACKAGE" KANTO_STORMFORGED)
          (cd dist && sha256sum "$CART" "$PACKAGE" > sha256sums.txt)
          cat dist/sha256sums.txt

      - name: Commit generated label to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add label.png
          if ! git diff --cached --quiet; then
            git commit -m "Generate STORMFORGED cartridge label [skip ci]"
            git push origin HEAD:main
          fi

      - name: Publish latest GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "dist/$CART" "dist/$PACKAGE" dist/sha256sums.txt --clobber
            gh release edit "$TAG" \
              --title "KANTO: STORMFORGED v$VERSION" \
              --notes-file RELEASE_NOTES.md \
              --latest
          else
            gh release create "$TAG" \
              --target main \
              --title "KANTO: STORMFORGED v$VERSION" \
              --notes-file RELEASE_NOTES.md \
              --latest \
              "dist/$CART" \
              "dist/$PACKAGE" \
              dist/sha256sums.txt
          fi
